{
  "name": "sajag-bharat",
  "version": "1.0.0",
  "description": "Sajag Bharat — Hindi news portal. Static HTML/CSS/JS frontend, Cloudflare Pages Functions backend, GitHub as the content + image store.",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "node scripts/generate.js",
    "dev": "npm run generate && npx wrangler pages dev public --compatibility-date=2026-01-01",
    "deploy": "npm run generate && npx wrangler pages deploy public"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
